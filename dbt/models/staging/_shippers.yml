version: 2

sources:
  - name: northwind
    database: "{{ target.database }}"
    schema: LANDING
    tables:
      - name: shippers
        columns:
          - name: SHIPPERID
          - name: COMPANYNAME
          - name: PHONE
